interface:
  display_name: "大宜宾选题与标题引擎"
  short_description: "普通主题拆新角度，每题直接配高点击标题；定题后继续精修标题"
  default_prompt: "Use $dayibin-topic-angle-engine to produce distinct topic-and-headline pairs with bold, deliverable hooks; for a locked angle, return ranked headline options without changing the topic."
policy:
  allow_implicit_invocation: true
