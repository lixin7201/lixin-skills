# 情感婚恋互动（兼容入口）

本文件只用于兼容旧路径。实际规则以 `情感婚恋互动DNA.md` 为准；读取本文件后必须转读 DNA 版，不得把两份内容叠加成两套路由。

关键边界：

- 默认短帖；明确要求长文或 800 字以上才走长文。
- 虚拟稿使用单一角色第一人称并保留 `【情感讨论·虚构情境】`。
- 非情感任务不得使用本类型的虚构参数、长文结构或安全规则。
- 高风险内容标记 `HUMAN_REVIEW_ONLY`，只生成候选，不自动发布。
